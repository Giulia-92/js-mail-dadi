function gioca() {
	let g1 = Math.round(Math.random()*5+1);
	let g2 = Math.round(Math.random()*5+1);
			
    if(g1 >= 10){
        prompt("hai vinto")
    }

           
	else{

    prompt("hai perso")
    }
	   
};